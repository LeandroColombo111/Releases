repos:
  - repo: local
    hooks:
      - id: commit-message-check
        name: Check commit message format
        entry: bash -c '[[ $(cat $1) =~ ^\[\DS-[0-9]+\]\ \-\ (feat|fix|chore|docs|style|refactor|perf|test|build|ci|revert):\ .+$ ]]'
        language: system
        stages: [commit-msg]
